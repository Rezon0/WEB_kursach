<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Client menu</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        div {
            margin: 20px;
        }

        form {
            display: inline-block;
            margin-right: 10px;
        }

        input[type="submit"] {
            padding: 10px;
            font-size: 16px;
            cursor: pointer;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
        }

        input[type="submit"]:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
<div>
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <input type="submit" value="Sign Out"/>
    </form>
</div>
<div>
    <form action="/client_menu/profile" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <div><input type="submit" value="Profile"/></div>
    </form>

    <form action="/client_menu/view_records" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <div><input type="submit" value="View records"/></div>
    </form>

    <form action="/client_menu/tattoo_zapis" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <div><input type="submit" value="Zapisatsya na seans"/></div>
    </form>

    <form action="/client_menu/tattoo_tattoo_care_products" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
        <div><input type="submit" value="View tattoo care products"/></div>
    </form>
</div>
</body>
</html>
